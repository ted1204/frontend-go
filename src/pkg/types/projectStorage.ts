// Deprecated: projectStorage types are now represented by groupStorage.
// Re-export groupStorage types for backward compatibility while the
// codebase migrates to `groupStorage` naming.
export * from './groupStorage';

